{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认权限。数据与文件不走 Tauri API（IndexedDB + WebView2 File 对象），只留 core 默认集；小窗窗口不调任何命令，只是跟着留一份",
  "windows": ["main", "mini"],
  "permissions": ["core:default"]
}
