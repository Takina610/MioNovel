# MioNovel — 给改这个仓库的 AI 的说明

本地优先的小说阅读 PWA（txt / epub → 书架 → 阅读器）。**为什么这么做**写在
`docs/SPEC.md`（那是一份决定记录，改代码之前该读的读一遍），这份只讲**动手时必须守的规矩**。

## 一、界面文案：只写具体事实

界面上**不许**出现下面这几类字：

- **操作指南**：「点一下文档就能进阅读器」·「行尾的 ⋯ 里是解析设置与删除」·
  「拖一本小说进来，或者点上面的「上传」」。按钮长得像按钮，不需要一句旁白教人点它。
- **自我表扬 / 免责声明**：「全是本地文件，不上传」·「不用再拖一次」这种讲卖点或讲实现的话。
- **功能罗列**：把选项再数一遍当副标题（「字距、行距、缩进都能调」）。
- **changelog**：「解析器更新之后（比如脚注、封面的改进）跑一遍就能……」。

留什么：**这一屏此刻是什么状态**（「还没有文档」·「没有匹配「雪」的文档」·「正在读目录…」），
以及**一个具体的事实**（「最近 3 个文件」·「已读 42%」）。一句能删就删；
删了会让人看不懂的才留。灰按钮的 `title` 例外——那里要写清为什么是灰的（见下），
但也就一句话（「这个外壳里没有」）。

想解释「为什么这么做」「怎么实现的」，写进代码注释和 `docs/SPEC.md`，不要放界面上。

## 二、五条硬约束（破了就不是这个项目了）

1. **颜色只能来自主题注册表。** 组件里不写色号，CSS 里不为某个主题写选择器，一律
   `var(--mn-*)` / 工具类（`bg-surface`、`text-fg-muted`）。唯一例外是**产品记号**
   （飞书的商标与卡片插图、MioNovel 自己的图），它们是有意写死的，且必须在注释里说明。
2. **组件只认 `chrome`（形态），不认主题 id。** 形态有八个：`plain` / `code`（VS Code）/
   `doc`（飞书）/ `chat`（企业微信）/ `page`（Word）/ `sheet`（Excel）/ `slide`（PPT）/
   `desk`（1688 客户工作台）。同一副外壳给亮暗两套主题用，一行都不用改。
3. **会响的按钮才可点，灰着的按钮不许响。** 按下去屏幕必须真的变（它们都是这个阅读器本来
   就有的设置：字号、行距、缩进、对齐、主题、摸鱼模式、换章、回书架……）；只读文档里本来
   就该灰的（粘贴、加粗、评论、云盘、知识库……）一律 `disabled` + `title` 说明原因。
   不画假菜单栏、假窗口控件、假输入框。
4. **不编造数据。** 时间、字数、进度、章数、作者、未读角标都是真的或算出来的。
   只有结构性计数器（行号、页码、幻灯片序号、`Ln 171, Col 26`）是生成的——它们说的是
   「这是第几段」，不声称是内容。没有作者就写「我」，不要编一个同事的名字。
5. **正文里不许插原文没有的字。** 唯一的例外是图片的引用行 `![](./路径)`——除了
   普通阅读器，其余六个形态的图都这么写（见下）。

## 三、几处容易踩的

- **图片**：**放真图的只有普通阅读器（`plain`）**，其余七个形态
  （编辑器、飞书、企业微信、表格、Word、幻灯片、客服工作台）都换成一行 `![](./路径)`
  （走 `lib/blocks.ts` 的 `mediaLinesHtml` / `chapterBlocks` 的 `'reference'`；
  路径必须是书里的原始路径，靠 `resolve` 把 blob 还原回来）。这条规矩**只在一处定义**：
  `lib/blocks.ts` 的 `mediaModeFor(chrome)`——历史上它写在两个调用点，改动只落到一处，
  于是「某个形态还在放图」被报了三回（企业微信、Word、幻灯片各一次，
  见决定记录 32 / 35 / 38）。加形态、改规矩都改那个函数，`verify:apps` 断言它。
- **改解析器之后**：存量的书不会自动变，要在书面板里重新解析。`bun run verify` 是回归防线。
- **CSS 的层级**：Tailwind 在 `@layer utilities` 里，手写 CSS（`styles/*.css`）是无层级的——
  所以手写规则能压过工具类。**别同时用两处给同一个元素定尺寸**（比如工具类给了 `h-4`，
  CSS 又写 `width: 20px`），改的时候容易只改一处，看不出效果。
- **图标尺寸**：我们的图标画在 24 格里、墨迹只占 60% 上下；要是照抄别家 16px 的图标盒子，
  看起来会小一圈。所以盒子要比「视觉大小」大 1.4 倍左右（24 的盒子 ≈ 16 的视觉大小）。
  量图标别把 `getBBox()` 的返回值当像素——它是 24 格坐标系里的**格数**。
- **窄屏**：列表/表格这类用 JS 算 `grid-template-columns` 的地方，收列也得由 JS 决定
  （CSS 覆盖不了 inline 样式，硬覆盖会让格子数和单元格数对不上）。
- **Word（`page`）那一屏的颜色**：纸边、裁剪标记、控件描边、搜索框底是四个专用 token
  （`--mn-page-*`，见 `themes/builtins.ts` 的 `page` 那一层），别在 `word.css` 里写色号；
  标题栏 / 页签行 / 桌面在 Word 里是**同一个色**（`bg`），只有功能面板是白的。
  纸上的留白（`--mn-word-margin` / `--mn-word-pad`）是**长度**不是百分比：百分比 padding
  按包含块算，写在纸上会大两倍多（踩过）。
- **功能区整条有宽度上限**：九组格子加起来得放得进 1920（Word 现在 1770、Excel 1820）。
  给某一格加宽、加一格之前先量一遍——多出来的那几十像素会把最后两组切到屏幕外（踩过）。
  `RibbonItem.kind` 说的是**这一格长什么样**（big / small / icon / text / rule / node / column），
  行与行之间不对齐是照 Word 来的；`dense: true` 是「同一个形状紧一档」（Excel 那十一组
  用它，图标 18、字 11.5、三格一摞正好 66）。
- **Excel（`sheet`）那一屏的颜色在 `--mn-sheet-*` 那一层**（见 `themes/builtins.ts` 的 `sheet`）：
  网格线、行列标题、选中框、控件描边都在那儿，别在 `excel.css` 里写色号。**两个绿不是一回事**：
  页签 / 按钮 / 药丸是品牌绿 `#107C41`（= `accent`），网格里那个选中框与「A」/「1」是
  Excel 的选中绿 `#217346`（= `--mn-sheet-select`），同一张截图里两个同时出现。
  标题栏、页签行、工作表标签条、开始屏幕左栏是**同一层** `--mn-sheet-chrome`（不是白的）。
- **网格后面补的那片空格子**：行高只在 `--mn-sheet-row` 一处定义（数据行、列标题行、
  补出来的空行都取它），补几行的 JS（`Content.tsx` 的 SheetGrid）按「还空着多少 / 行高」算，
  **基准高度要扣掉已经画上去的空行**，否则会来回抖；用 floor 不用 ceil，多一行就会多出滚动条。
- **章内查找（`lib/find.ts`）只准标，不准改原文**：命中的处理是套一个
  `<mark class="mn-find">`，清掉时把文字还给父节点再 `normalize()`。改完跑 `verify:apps`
  那 17 条断言——「标记吃掉一个字」这种事盯着屏幕看不出来。
- **一个类名只给一个地方用**。`.mn-doc__tool` 既当过飞书首页「筛选 / 显示设置」的触发器、
  又是阅读器工具条那一格，结果首页那两个按钮被工具条的尺寸（30×30 的网格）压扁，
  `nowrap` 的字溢出来压在右边的视图切换上。复用一个类名之前，先看另一边给它定过什么尺寸。
- **下拉菜单要挂在 `document.body` 上（portal + fixed）**：功能区的面板（`.mn-ribbon__panel`）
  与侧栏都是 `overflow` 容器，绝对定位的下拉会被它们**裁掉**（症状：字体下拉只剩两行、
  还被下排按钮压住——用户报的「下拉框层级出错」）。`OfficeFrame` 的 `AppMenu` 和
  `ui/Select` 都是这一套：位置由 JS 按触发器的 `getBoundingClientRect()` 算，
  滚动/改窗口时跟着走。
- **弹性项目里的中文标签要把 `min-width: 0` 一起写上。** 客服工作台那一屏的档案里，
  标签列量出来是 46px（`flex: 0 0 46px`），但浏览器渲染成 56——弹性项目的 `min-width`
  默认是 `auto`（= min-content），「书籍身份」四个字就是 56，于是那一条被顶宽、右边的值
  整列移位。截图量的值配 `flex-basis` 时，**顺手把 `min-width: 0` 写上**（配 `overflow: hidden`）。
- **会压暗正文的那一层要自己挂 `mn-veil`**：`--mn-dim` 只写在 `.mn-office` / 外壳根上，
  真正变暗靠的是某一块带 `.mn-veil` 的元素。三件套的正文容器（`.mn-word__body` /
  `.mn-excel__body` / `.mn-ppt__body`）漏挂过一次，于是「摸鱼在正文部分不生效」。
- **加一个快捷键 = 表里加一条 + 页面上登记一次**：`lib/hotkey.ts` 的 `HOTKEY_COMMANDS`
  里加一条（id / 标签 / 说明 / 默认组合（可以是**多个**）/ scope），再在页面上用
  `hooks/useHotkeys` 的 `useHotkey(id, handler)` 登记。设置面板那一栏、菜单上的键位提示、
  冲突检查都从这张表推出来。`scope: 'focused'` 允许单键（避开输入框），`'global'` 必须带修饰键
  （在输入框里也响）。翻页键（next-page / prev-page）登记在 ReaderView——翻列和滚动
  是它内部的事；退出阅读（exit，默认 Esc）登记在 ReaderPage，它要让位给一切挂着
  `data-mn-esc-local` 的局部界面（Select 下拉、AppMenu、Word 沉浸、PPT 阅读视图）——
  给某个局部界面占住 Esc，就在它的元素上挂这个标记。
- **验键盘行为时**：合成的 `new KeyboardEvent('keydown', {...})` 默认 `cancelable: false`，
  于是 `preventDefault()` 静默失效、`defaultPrevented` 永远是 false——照它判会得出
  「快捷键没生效」的错误结论。自己造事件要显式写 `cancelable: true`。
- **主题封面是截图，类别从 chrome 推导**：`src/assets/theme-shots/<主题 id>.webp`
  （2560×1360 的原图用 sharp 压到 440 宽 / q50，20 张约 125KB；ThemePicker 用
  `import.meta.glob` 按文件名取，新主题没图不报错、回落色卡，`verify:apps` 会点名缺图）。
  类别（常规 / 编辑器 / 通讯 / Office）定义在 `themes/groups.ts`，按 chrome 归组——
  别在 ThemePicker 里按主题 id 硬排，那样新主题会落不进任何一组。
  非「常规」主题名称左侧的**产品商标**在 `src/assets/logos/`（官方渠道的真实彩色
  图标，产品记号例外、亮暗主题共用；别自己画，也别换成单色线条版）。

## 四、动完手必须过的三关

```bash
bun run typecheck   # tsconfig 是 strict，别留未使用的变量
bun run verify      # 解析器 + 演示模式 + 快捷键 + 办公外壳（新增「不看屏幕发现不了」的逻辑就补断言）
bun run build       # 顺手确认 PWA 那条链没断
```

界面改动还要在浏览器里当读者走一遍（导入 → 开书 → 翻章 → 换主题 → 缩到 390px），
别只看数据对。
