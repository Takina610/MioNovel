node_modules/
dist/
.vite/
.cache/
*.local
.DS_Store
Thumbs.db
desktop.ini

# 验收用的样例文件由 `bun run samples` 生成，不进仓库
samples/

# Rust 构建产物与 Tauri 生成的权限 schema（src-tauri/.gitignore 也挡了一道）
src-tauri/target/
src-tauri/gen/

# 编辑器 / 工具的运行状态，不属于项目源码
.zcode/

# 注意：public/ 里的图标（favicon / apple-touch / maskable / pwa-*）是 `bun run icons`
# 从 MioNovel.png 生成的，logo-64 / logo-192 / favicon.svg 是 `bun run logo` 派生的，
# 但它们都要进仓库——这样 clone 下来不用先装 pwa-assets-generator 就能构建。
